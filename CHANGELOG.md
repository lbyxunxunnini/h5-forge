# Changelog

## 0.2.3

0.2.3 是一次面向“确认粒度”和“UI 输出节奏”的细化版本，重点收紧用户确认的生效范围，并把新项目共创里的 UI 输出拆成结构草案和完整结构两步。

- 补强确认绑定规则：用户一句“确认 / 可以 / 按这个来 / 没问题”只对上一轮明确摘要生效，不能自动吞掉摘要外的未决项
- 补强确认回写要求：如果仍有未决项，必须先回写“已确认 / 待确认”，再继续追问关键问题
- 补强 UI 首轮输出闸门：新项目共创模式下，UI 设计师在方向确认后的第一轮只允许输出“结构草案”，不得一次性展开完整页面结构包
- 更新角色交接格式：新增“结构草案确认状态”，明确未通过时禁止下发完整 UI 包
- 同步更新主规则、运行时提示、共享确认门禁和 README 说明

## 0.2.2

0.2.2 是一次面向规则抽象和跨项目同步效率的版本，重点把顾问式协作和老项目顾问提问沉淀成共享工作流规则。

- 新增 `references/shared_workflow_gates/advisor_collaboration.md`，抽取四角色统一的顾问式协作规则
- 新增 `references/shared_workflow_gates/legacy_project_consulting.md`，抽取老项目扫描、复用判断、规则冲突和改动边界不清时的顾问式提问规则
- 更新 `shared_workflow_gates/README.md`，把共享工作流主题扩展到顾问式协作和老项目顾问提问
- 主规则同步强化：老项目模式下也要求先给默认建议和原因，再提出关键确认问题
- 老项目入口与扫描规则同步强化：迭代中项目不再只是扫描后执行，而是按顾问式提问方式推进关键确认

## 0.2.1

0.2.1 是一次面向“共创体验”的细化版本，重点补强了新项目共创后半段的确认闸门，以及四个角色统一的顾问式协作方式。

- 补强新项目共创后半段：需求分析师每轮必须带推荐建议，UI 设计师和架构设计师新增确认闸门，避免后半段一次性倾倒完整方案
- 明确四个角色都应按顾问式协作工作，不只是需求分析师；其中需求分析师最主动，页面工程师也需要提供第一版实现建议和实现层取舍建议
- 补强页面工程师角色卡：从被动执行改为实现层顾问，要求在新项目共创模式下先给“第一版实现建议”，再展开详细实现步骤
- 新增 UI 方向确认状态和架构落地确认状态，要求在进入下一个角色前明确是否允许继续下发

## 0.2.0

0.2.0 是一次面向工作流稳定性的升级版本，重点补强了路由纠偏、需求确认闸门、新项目共创模式，以及跨项目可复用的共享门禁规则。

- 支持误路由纠正：用户指出缺少 UI/架构/需求分析接入时，直接切回正确流程，不再二次询问是否升级
- 补强需求阶段纠偏：用户明确说“这是一个需求”“需要需求分析”“重新做需求分析”时，必须直接回到需求分析师，不得继续页面工程师执行态
- 补强需求分析停机条件：如果歧义会影响核心页面的形态、模板复用、内容源、路由、首屏目标、返回路径、SEO 或主要交互，必须先向用户提问，不能自动把默认值下发到 UI/架构/实现
- 补强需求确认闸门：需求分析完成后，只有在用户确认状态为“允许进入下一角色”时，才允许切到 UI/架构；首页、详情页、列表页、表单页、交易页、内容页等核心页面必须先讨论展示字段、区块组成和内容逻辑
- 泛化续写触发示例：移除项目专属关键词，改为通用页面/模块/详情页/交互表达，避免把 h5-forge 误收窄到单一业务场景
- 重构触发系统为“硬触发 + 语义触发 + 示例表达”三层结构，减少关键词堆积导致的误判和维护成本
- 新增 `new_project_cocreation_mode.md`，支持从模糊想法出发，通过多轮问答逐步收口需求、风格和页面结构，再落地成新项目
- 新增 `references/shared_workflow_gates/` 子目录，沉淀可在 H5 Forge 与 Flutter Forge 之间直接复制的通用门禁规则
- 收紧 10 秒测试：从需求/设计开始的新页面、新模块、新项目任务优先进入完整流程，不能因描述清晰而误判为轻量任务

## 0.1.0

H5 Forge 首个公开版本，基于 Flutter Forge 的工作流形态改造为 H5/Web 项目协作 skill。

- 新增 H5/Web 项目主控 `SKILL.md`
- 支持“硬排除 + 运维直通 + 轻量执行 + 完整流程”的任务路由
- 支持已有 H5/Web 项目接入和新 H5/Web 应用 `mvp / business / team` profile
- 支持规则卡、会话状态、相似实现检索、输入不完整处理和完整流程升级原因
- 支持误路由纠正：用户指出缺少 UI/架构接入时，直接切回正确流程，不再二次询问是否升级
- 补强需求阶段纠偏：用户明确说“这是一个需求”“需要需求分析”“重新做需求分析”时，必须直接回到需求分析师，不得继续页面工程师执行态
- 补强需求分析停机条件：如果歧义会影响核心页面的形态、模板复用、内容源、路由、首屏目标、返回路径、SEO 或主要交互，必须先向用户提问，不能自动把默认值下发到 UI/架构/实现
- 补强需求确认闸门：需求分析完成后，只有在用户确认状态为“允许进入下一角色”时，才允许切到 UI/架构；首页、详情页、列表页、表单页、交易页、内容页等核心页面必须先讨论展示字段、区块组成和内容逻辑
- 泛化续写触发示例：移除博客项目专属关键词，改为通用页面/模块/详情页/交互表达，避免把 h5-forge 误收窄到单一业务场景
- 新增 `references/shared_workflow_gates/` 子目录，沉淀可在 H5 Forge 与 Flutter Forge 之间直接复制的通用门禁规则
- 新增 `new_project_cocreation_mode.md`，支持从模糊想法出发，通过多轮问答逐步收口需求、风格和页面结构，再落地成新项目
- 重构触发系统为“硬触发 + 语义触发 + 示例表达”三层结构，减少关键词堆积导致的误判和维护成本
- 补强新项目共创后半段：需求分析师每轮必须带推荐建议，UI 设计师和架构设计师新增确认闸门，避免后半段一次性倾倒完整方案
- 补强老项目顾问式提问：迭代中项目扫描、复用判断、规则冲突、改动边界不清时，也必须先给默认建议和原因，再提出关键确认问题
- 收紧 10 秒测试：从需求/设计开始的新页面、新模块、新项目任务优先进入完整流程，不能因描述清晰而误判为轻量任务
- 新增前端项目质量、路由、网络层、测试、i18n/a11y、代码审查和迁移参考文档
- 新增 MIT License、贡献指南、GitHub issue 模板和开源发布检查清单
- 新增 `scripts/discover_h5_skills.sh`，用于探测本地可协作的前端 skills
