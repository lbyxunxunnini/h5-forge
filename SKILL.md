---
name: h5-forge
description: >-
  H5/Web 项目开发主控 skill。用户显式使用 "h5f-"、"h5f-fast"、"h5f-a"、"/h5-forge"、"使用 h5-forge"、"按 h5-forge 工作模式处理" 等方式触发时进入。也可在用户明确要求按 H5/Web 项目结构化流程处理时进入。
  硬触发关键词："h5f-fast"、"h5f-a"、"h5f a"、"h5f-"、"h5f -"、"h5f "、"h5-forge"、"/h5-forge"、"使用 h5-forge"、"按 h5-forge 工作模式处理"。用户输入以 "h5f" 开头时必须触发。
  命中后由 controller 在当前整轮任务内主动分流；文档、代码环境、打包、CI/CD、闲聊等走直通模式，任务完成后自动退出。需要专项判断时再展示需求分析师、UI 设计师、架构设计师、页面工程师标签。
---

# H5 Forge

H5/Web 项目内的 `controller` skill。先路由，再分类，再执行。

## 核心原则

1. `controller` 唯一——命中判断、任务路由、阶段推进、升级降级、最终输出
2. 先路由 → 再分类 → 再展示模式 → 再执行
3. 任务类型和任务规模分离（`大任务` 只代表复杂度）
4. `完整流程` 是执行协议，不是用户可见模式名
5. 角色标签按需输出，只在需要专项判断时展示
6. 提问由 controller 排序合并，由对应角色发出，每轮只问 1 个
7. UI 关键信息不足时先补输入，不直接拍板
8. 大文档先压缩成阶段摘要包，不整包下发
9. session = 当前任务状态；规则卡 = 项目长期约束
10. 一整轮任务完成后自动退出，不跨轮常驻
11. `h5f-fast` 是快速执行策略：轻量优先，自动生成扫描摘要，只有发现明确风险才升级
12. `h5f-a` 是全自动执行策略：非阻塞缺口采用推荐方案继续推进，高风险或不可逆事项才中断确认

## 命中路由

### 进入条件

满足任一：硬触发词 `h5f-fast`/`h5f-a`/`h5f-`/`/h5-forge`/`h5-forge`、用户明确要求按结构化流程处理。

仅因为当前工作区是 H5/Web 项目，不自动进入。

### 快速入口 `h5f-fast`

`h5f-fast` 是执行策略，不是独立任务模式。命中后优先按轻量 / 中等路径处理，并生成项目扫描摘要；只有发现明确需求缺口、UI 结构决策或架构边界风险时才升级。

快速策略下：
- 轻量任务最多读 1-3 个关键文件，最多输出开始 / 完成 2 条主要日志
- 中等任务执行“15 分钟内完成策略”：扫描相似实现 → 直接改 → 最小验证
- 启动时可调用 `scripts/project_snapshot.py` 生成冷启动摘要
- 需要结构性决策时必须升级，不为速度绕过阶段门禁

详细规则见 [fast_mode.md](references/fast_mode.md)。

### 全自动入口 `h5f-a`

`h5f-a` 是全自动执行策略，不是独立任务模式。命中后先输出全自动日志，再继续按正常路由进入直通 / 轻量 / 中等 / UI 优化 / 架构级 / 功能开发 / 页面开发 / 新项目共创。

全自动策略下：
- 普通需求、UI、架构缺口不反复问用户，controller 选择推荐方案并标记为 `auto_assumption`
- 自动方案必须来自规则卡、相似实现、项目主流模式或 H5/Web 常规工程实践
- 阶段门禁仍存在，只是“用户确认”可由“推荐方案自动确认”替代
- 安全、隐私、生产环境、删除数据、不可逆迁移、全项目架构切换、write scope 不清晰时必须中断确认

详细规则见 [autonomous_mode.md](references/autonomous_mode.md)。

### 路由顺序

命中后按此顺序判定（命中即停）：

1. 直通模式？（文档/环境/打包/CI/CD/闲聊/快速确认）→ 直通
2. 新项目共创？（模糊想法/空项目/讨论方向阶段）→ 新项目共创
3. 优化场景？→ 需求+优化→功能开发 / UI优化→UI优化任务 / 架构优化→架构级任务
4. 架构级任务？（代码审查/迁移/重构/依赖清理/i18n-a11y/性能优化）
5. 页面开发？（新页面/新模块/页面结构与路由接入）
6. 功能开发？（大功能闭环/跨页面状态联动/弹窗+提示栏+深链+业务流程）
7. 从需求或设计起步？→ 页面开发或功能开发（按任务语义）
8. 通过 10 秒测试？→ 轻量任务
9. 中间地带？→ 中等任务
10. 其他 → 功能开发

### 从需求/设计起步的硬规则

满足任一时直接进入完整流程，不做轻量降级：
- 用户从 PRD/需求说明/设计方向/页面规划开始
- 用户要求"先设计""先拆结构"
- 新页面/新模块/从 0 到 1
- 第一步本来就应当是需求理解或设计

### 10 秒测试

同时满足才允许走轻量：
- 不是从需求/设计起步
- 用户已指明具体文件/组件/属性（不是 controller 推断）
- 不涉及组件边界、状态管理、路由、模块归属、复用决策

判定主体：以用户输入为准。

## 执行协议

### 双轨模型

- 共创轨道 `C0-C3`：新项目共创
- 执行轨道 `S0-S6`：开发执行
- `C3` 完成后进入 `S3`（C0-C3 等价于 S1+S2，不重复走）
- 已有项目默认从 `S1` 开始

#### S0 未收口（内部状态，不对外输出）

路由判定完成但需补上下文（读规则卡、扫描）时短暂经过。轻量/中等任务跳过。满足后自动进入 S1。

#### C3 → S3 等价性

- C0-C1 ≈ S1（目标、场景、结构方向已收口）
- C2-C3 ≈ S2（工程组织、状态管理、路由、首批范围已冻结）
- 首批范围足够小时可跳过 S3 直接进入 S4

### 各模式执行要点

| 模式 | 执行链 | 关键约束 |
|------|--------|----------|
| 直通 | 主控直做 | 不展开专项，子任务完成退出当前任务（非退出会话） |
| 轻量 | 读→改→验证 | 不走握手，不拉专项 |
| 中等 | 扫描→短判断→执行或升级 | 升级时输出新模式+原因+从对应阶段继续 |
| UI 优化 | 范围确认→UI判断→实现 | 用户已说清范围可跳过确认；缺视觉输入先补要 |
| 架构级 | 架构判断→实现→验证 | 涉及业务约束时先进 S1 |
| 功能开发 | S1→S2→S4→S5 | 补升级原因；大文档先压缩摘要包 |
| 页面开发 | S1→S2→S4→S5 | 补升级原因；缺 UI 信息先补要 |
| 新项目共创 | C0→C1→C2→C3→S3 | C3 前禁止实现 |

`h5f-fast` 启用时，上表优先按轻量 / 中等执行；发现明确风险时输出升级原因并进入对应重流程。

`h5f-a` 启用时，上表中的“确认 / 补要”优先改为“采用推荐方案并记录自动假设”；只有命中高风险中断条件才向用户确认。

详细执行逻辑见 [task_runtime_prompt.md](references/task_runtime_prompt.md)

### 中等任务升级路径

- 需求缺口 → 功能开发/页面开发，从 S1 开始
- UI 结构决策 → UI 优化/页面开发，从 S2 开始
- 架构边界风险 → 架构级任务，从 S2 开始

### 大任务并行协议

前提：宿主支持真实子代理并行（见 [host_subagent_support.md](references/host_subagent_support.md)），否则降级为串行工作包。

`主控 → arch-agent 拆包 → 多 impl-agent 并行 → verify-agent 收口`

- S3：拆分包必须包含任务名、冻结约束、工作包列表（owner/scope/禁止区域/done 定义）
- S4：5 项前提全满足才并行；阻塞回传后主控评估影响范围（仅当前包/共享约束/上游变化）
- S5：统一收口验证，未通过不进 S6

并行阻塞联动：
- 仅当前包受影响：其他继续，当前暂停
- 共享约束受影响：受影响包暂停，标记 stale_results
- 上游方案变化：回退 S2/S3，全部结果待确认

### 多任务边界

- "一轮任务"以用户一次完整意图为单位
- 直通子任务完成后新任务重新走路由
- 用户本轮无新任务时才真正退出 h5-forge

## 阶段门禁

1. 需求未确认且无 `auto_assumption` → 禁止实现
2. UI/架构未稳定且无 `auto_assumption` → 禁止实现
3. 拆包未冻结 → 禁止并行
4. 上游变化 → 下游结果失效
5. UI 关键信息不足 → 普通模式禁止跳过补充；`h5f-fast` 发现该问题时必须升级或中断确认；`h5f-a` 可采用推荐 UI 草案但必须标注来源
6. 多角色缺口 → controller 排序后一问一答
7. 上游未收口 → 下游不得宣布冻结
8. 大文档 → 禁止整包下发

用户要求阶段回退时，先回退再继续。

## 误路由纠正

用户指出模式不对时：承认错误 → 输出正确模式标志 → 补升级原因 → 直接进入纠正后阶段。

## 输出日志

`[h5-forge]` 必选。日志分 3 层：模式日志 → 阶段日志（变化时才输出）→ 结果日志（只展示真实参与角色）。

角色标签：`需求分析师` / `UI 设计师` / `架构设计师` / `页面工程师` / `验证工程师`

完整格式和示例见 [skill_visibility.md](references/skill_visibility.md)

## 按需加载

| 场景 | 加载 |
|------|------|
| 决策/提问/门禁细则 | [decision_and_question_protocol.md](references/decision_and_question_protocol.md) |
| 快速执行策略 | [fast_mode.md](references/fast_mode.md) |
| 全自动执行策略 | [autonomous_mode.md](references/autonomous_mode.md) |
| 运行时检查 | [task_runtime_prompt.md](references/task_runtime_prompt.md) |
| 可见性/日志格式 | [skill_visibility.md](references/skill_visibility.md) |
| Session 管理 | [session_management.md](references/session_management.md) |
| 规则卡协议 | [rule_card_protocol.md](references/rule_card_protocol.md) |
| 规则卡校验 | [rule_card_validation.md](references/rule_card_validation.md) |
| 项目初始化 | [project_init_flow.md](references/project_init_flow.md) |
| 新项目共创 | [new_project_cocreation_mode.md](references/new_project_cocreation_mode.md) |
| 宿主子代理支持 | [host_subagent_support.md](references/host_subagent_support.md) |
| 角色契约 | [roles/](references/roles/) 目录下各文件 |
| 完整加载映射 | [load_map.md](references/load_map.md) |
| 前端协作 skills 探测 | [frontend_skills.md](references/frontend_skills.md) |
| H5 技术栈识别 | [h5_stack_detection.md](references/h5_stack_detection.md) |
| 技术栈 profile | [stack_profiles.md](references/stack_profiles.md) |

## 规则优先级

### P0 硬规则（违反即错误）

- 阶段门禁
- 角色边界（impl 不重定义需求，ui 不决定状态管理，requirement 不给架构结论）
- 一问一答（每轮只问 1 个）
- 10 秒测试以用户输入为准
- 宿主能力未知时不假装并行
- 进入后必须输出 `[h5-forge]` 模式日志
- `h5f-fast` 不能绕过阶段门禁、验证或高风险确认
- `h5f-a` 不能自动执行安全、生产、删除数据、不可逆迁移或全项目架构切换类高风险决策

### P1 核心规则（应当遵循）

- 升级原因必须具体
- 角色标签精确归属
- 大文档先压缩摘要包
- session 阶段切换时写
- 大任务结束时判断规则卡出口
- UI 信息不足先补要

### P2 建议规则（推荐但不强制）

- 相似实现搜索
- 结果日志结构化
- 共创分步输出
- 验证强度分级

冲突解决：P0 > P1 > P2；用户显式指令 > 任何规则（除安全红线）。

## 阶段转换自检

每次阶段转换前，controller 内部过一遍：

1. 要进入什么阶段？
2. 前置条件是什么？
3. 现在满足吗？
4. 不满足缺什么？回到哪？

| 转换 | 自检 |
|------|------|
| → 进入模式 | 是否满足进入条件？是否误判轻量？ |
| S1→S2 | 放行条件全满足？ |
| S2→S3/S4 | 方案稳定？上游无未收口？ |
| S3→S4 | 拆分包完整？宿主支持？值得并行？ |
| S4→S5 | 工作包全完成或已标失效？ |
| S5→S6 | 验证通过？无越界？共享约束成立？ |
| C3→S3 | C0-C3 产出覆盖 S1+S2？ |

失败时：不跳过，输出阻塞原因，回到正确阶段。

## 红线

1. 需求/方案未确认时禁止实现
2. 轻量任务必须同时满足 10 秒测试 + 非需求/设计起步
3. 任务规模只判断复杂度，不决定角色输出数量
4. 实现层禁止改写需求边界和架构结论
5. 只输出当前阶段必需的日志
